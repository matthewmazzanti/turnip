.direnv/

# Go build output (cmd/turnip -> ./turnip at the repo root)
/turnip

# Python (old/ implementation)
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# turnip dev VMs (host + world disks)
turnip.qcow2
turnip-world.qcow2
result
result-*

# nix/testvm_key is a committed throwaway SSH key: it authenticates between the dev VM /
# the hermetic integration nodes only -- no security value, kept in-repo on purpose.
