.direnv/

# Go build output (cmd/turnip -> ./turnip at the repo root)
/turnip

# Python (old/ implementation)
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# turnip dev-VM state: disks, console logs, QMP sockets (nix/vm.sh writes here).
# Anchored to the repo root -- the state dir is $root/vm; nix/vm/ (the VM modules) is tracked.
/vm/
result
result-*

# nix/vm/testvm_key is a committed throwaway SSH key: it authenticates between the dev VMs /
# the hermetic integration nodes only -- no security value, kept in-repo on purpose.
*.qcow2
